// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/bengobox/iot-service/internal/ent/device"
	"github.com/bengobox/iot-service/internal/ent/devicecommand"
	"github.com/google/uuid"
)

// DeviceCommand is the model entity for the DeviceCommand schema.
type DeviceCommand struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// TenantID holds the value of the "tenant_id" field.
	TenantID uuid.UUID `json:"tenant_id,omitempty"`
	// DeviceID holds the value of the "device_id" field.
	DeviceID uuid.UUID `json:"device_id,omitempty"`
	// CommandType holds the value of the "command_type" field.
	CommandType string `json:"command_type,omitempty"`
	// Payload holds the value of the "payload" field.
	Payload map[string]interface{} `json:"payload,omitempty"`
	// Status holds the value of the "status" field.
	Status string `json:"status,omitempty"`
	// SentAt holds the value of the "sent_at" field.
	SentAt time.Time `json:"sent_at,omitempty"`
	// AcknowledgedAt holds the value of the "acknowledged_at" field.
	AcknowledgedAt time.Time `json:"acknowledged_at,omitempty"`
	// Response holds the value of the "response" field.
	Response map[string]interface{} `json:"response,omitempty"`
	// Metadata holds the value of the "metadata" field.
	Metadata map[string]interface{} `json:"metadata,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the DeviceCommandQuery when eager-loading is set.
	Edges        DeviceCommandEdges `json:"edges"`
	selectValues sql.SelectValues
}

// DeviceCommandEdges holds the relations/edges for other nodes in the graph.
type DeviceCommandEdges struct {
	// Device holds the value of the device edge.
	Device *Device `json:"device,omitempty"`
	// History holds the value of the history edge.
	History []*CommandHistory `json:"history,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// DeviceOrErr returns the Device value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e DeviceCommandEdges) DeviceOrErr() (*Device, error) {
	if e.Device != nil {
		return e.Device, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: device.Label}
	}
	return nil, &NotLoadedError{edge: "device"}
}

// HistoryOrErr returns the History value or an error if the edge
// was not loaded in eager-loading.
func (e DeviceCommandEdges) HistoryOrErr() ([]*CommandHistory, error) {
	if e.loadedTypes[1] {
		return e.History, nil
	}
	return nil, &NotLoadedError{edge: "history"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*DeviceCommand) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case devicecommand.FieldPayload, devicecommand.FieldResponse, devicecommand.FieldMetadata:
			values[i] = new([]byte)
		case devicecommand.FieldCommandType, devicecommand.FieldStatus:
			values[i] = new(sql.NullString)
		case devicecommand.FieldSentAt, devicecommand.FieldAcknowledgedAt:
			values[i] = new(sql.NullTime)
		case devicecommand.FieldID, devicecommand.FieldTenantID, devicecommand.FieldDeviceID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the DeviceCommand fields.
func (dc *DeviceCommand) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case devicecommand.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				dc.ID = *value
			}
		case devicecommand.FieldTenantID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field tenant_id", values[i])
			} else if value != nil {
				dc.TenantID = *value
			}
		case devicecommand.FieldDeviceID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field device_id", values[i])
			} else if value != nil {
				dc.DeviceID = *value
			}
		case devicecommand.FieldCommandType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field command_type", values[i])
			} else if value.Valid {
				dc.CommandType = value.String
			}
		case devicecommand.FieldPayload:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field payload", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &dc.Payload); err != nil {
					return fmt.Errorf("unmarshal field payload: %w", err)
				}
			}
		case devicecommand.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				dc.Status = value.String
			}
		case devicecommand.FieldSentAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field sent_at", values[i])
			} else if value.Valid {
				dc.SentAt = value.Time
			}
		case devicecommand.FieldAcknowledgedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field acknowledged_at", values[i])
			} else if value.Valid {
				dc.AcknowledgedAt = value.Time
			}
		case devicecommand.FieldResponse:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field response", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &dc.Response); err != nil {
					return fmt.Errorf("unmarshal field response: %w", err)
				}
			}
		case devicecommand.FieldMetadata:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field metadata", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &dc.Metadata); err != nil {
					return fmt.Errorf("unmarshal field metadata: %w", err)
				}
			}
		default:
			dc.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the DeviceCommand.
// This includes values selected through modifiers, order, etc.
func (dc *DeviceCommand) Value(name string) (ent.Value, error) {
	return dc.selectValues.Get(name)
}

// QueryDevice queries the "device" edge of the DeviceCommand entity.
func (dc *DeviceCommand) QueryDevice() *DeviceQuery {
	return NewDeviceCommandClient(dc.config).QueryDevice(dc)
}

// QueryHistory queries the "history" edge of the DeviceCommand entity.
func (dc *DeviceCommand) QueryHistory() *CommandHistoryQuery {
	return NewDeviceCommandClient(dc.config).QueryHistory(dc)
}

// Update returns a builder for updating this DeviceCommand.
// Note that you need to call DeviceCommand.Unwrap() before calling this method if this DeviceCommand
// was returned from a transaction, and the transaction was committed or rolled back.
func (dc *DeviceCommand) Update() *DeviceCommandUpdateOne {
	return NewDeviceCommandClient(dc.config).UpdateOne(dc)
}

// Unwrap unwraps the DeviceCommand entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (dc *DeviceCommand) Unwrap() *DeviceCommand {
	_tx, ok := dc.config.driver.(*txDriver)
	if !ok {
		panic("ent: DeviceCommand is not a transactional entity")
	}
	dc.config.driver = _tx.drv
	return dc
}

// String implements the fmt.Stringer.
func (dc *DeviceCommand) String() string {
	var builder strings.Builder
	builder.WriteString("DeviceCommand(")
	builder.WriteString(fmt.Sprintf("id=%v, ", dc.ID))
	builder.WriteString("tenant_id=")
	builder.WriteString(fmt.Sprintf("%v", dc.TenantID))
	builder.WriteString(", ")
	builder.WriteString("device_id=")
	builder.WriteString(fmt.Sprintf("%v", dc.DeviceID))
	builder.WriteString(", ")
	builder.WriteString("command_type=")
	builder.WriteString(dc.CommandType)
	builder.WriteString(", ")
	builder.WriteString("payload=")
	builder.WriteString(fmt.Sprintf("%v", dc.Payload))
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(dc.Status)
	builder.WriteString(", ")
	builder.WriteString("sent_at=")
	builder.WriteString(dc.SentAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("acknowledged_at=")
	builder.WriteString(dc.AcknowledgedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("response=")
	builder.WriteString(fmt.Sprintf("%v", dc.Response))
	builder.WriteString(", ")
	builder.WriteString("metadata=")
	builder.WriteString(fmt.Sprintf("%v", dc.Metadata))
	builder.WriteByte(')')
	return builder.String()
}

// DeviceCommands is a parsable slice of DeviceCommand.
type DeviceCommands []*DeviceCommand
