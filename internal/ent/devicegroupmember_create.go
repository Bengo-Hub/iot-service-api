// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"time"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/bengobox/iot-service/internal/ent/device"
	"github.com/bengobox/iot-service/internal/ent/devicegroup"
	"github.com/bengobox/iot-service/internal/ent/devicegroupmember"
	"github.com/google/uuid"
)

// DeviceGroupMemberCreate is the builder for creating a DeviceGroupMember entity.
type DeviceGroupMemberCreate struct {
	config
	mutation *DeviceGroupMemberMutation
	hooks    []Hook
}

// SetDeviceGroupID sets the "device_group_id" field.
func (dgmc *DeviceGroupMemberCreate) SetDeviceGroupID(u uuid.UUID) *DeviceGroupMemberCreate {
	dgmc.mutation.SetDeviceGroupID(u)
	return dgmc
}

// SetDeviceID sets the "device_id" field.
func (dgmc *DeviceGroupMemberCreate) SetDeviceID(u uuid.UUID) *DeviceGroupMemberCreate {
	dgmc.mutation.SetDeviceID(u)
	return dgmc
}

// SetAddedAt sets the "added_at" field.
func (dgmc *DeviceGroupMemberCreate) SetAddedAt(t time.Time) *DeviceGroupMemberCreate {
	dgmc.mutation.SetAddedAt(t)
	return dgmc
}

// SetNillableAddedAt sets the "added_at" field if the given value is not nil.
func (dgmc *DeviceGroupMemberCreate) SetNillableAddedAt(t *time.Time) *DeviceGroupMemberCreate {
	if t != nil {
		dgmc.SetAddedAt(*t)
	}
	return dgmc
}

// SetID sets the "id" field.
func (dgmc *DeviceGroupMemberCreate) SetID(u uuid.UUID) *DeviceGroupMemberCreate {
	dgmc.mutation.SetID(u)
	return dgmc
}

// SetNillableID sets the "id" field if the given value is not nil.
func (dgmc *DeviceGroupMemberCreate) SetNillableID(u *uuid.UUID) *DeviceGroupMemberCreate {
	if u != nil {
		dgmc.SetID(*u)
	}
	return dgmc
}

// SetDeviceGroup sets the "device_group" edge to the DeviceGroup entity.
func (dgmc *DeviceGroupMemberCreate) SetDeviceGroup(d *DeviceGroup) *DeviceGroupMemberCreate {
	return dgmc.SetDeviceGroupID(d.ID)
}

// SetDevice sets the "device" edge to the Device entity.
func (dgmc *DeviceGroupMemberCreate) SetDevice(d *Device) *DeviceGroupMemberCreate {
	return dgmc.SetDeviceID(d.ID)
}

// Mutation returns the DeviceGroupMemberMutation object of the builder.
func (dgmc *DeviceGroupMemberCreate) Mutation() *DeviceGroupMemberMutation {
	return dgmc.mutation
}

// Save creates the DeviceGroupMember in the database.
func (dgmc *DeviceGroupMemberCreate) Save(ctx context.Context) (*DeviceGroupMember, error) {
	dgmc.defaults()
	return withHooks(ctx, dgmc.sqlSave, dgmc.mutation, dgmc.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (dgmc *DeviceGroupMemberCreate) SaveX(ctx context.Context) *DeviceGroupMember {
	v, err := dgmc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (dgmc *DeviceGroupMemberCreate) Exec(ctx context.Context) error {
	_, err := dgmc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (dgmc *DeviceGroupMemberCreate) ExecX(ctx context.Context) {
	if err := dgmc.Exec(ctx); err != nil {
		panic(err)
	}
}

// defaults sets the default values of the builder before save.
func (dgmc *DeviceGroupMemberCreate) defaults() {
	if _, ok := dgmc.mutation.AddedAt(); !ok {
		v := devicegroupmember.DefaultAddedAt()
		dgmc.mutation.SetAddedAt(v)
	}
	if _, ok := dgmc.mutation.ID(); !ok {
		v := devicegroupmember.DefaultID()
		dgmc.mutation.SetID(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (dgmc *DeviceGroupMemberCreate) check() error {
	if _, ok := dgmc.mutation.DeviceGroupID(); !ok {
		return &ValidationError{Name: "device_group_id", err: errors.New(`ent: missing required field "DeviceGroupMember.device_group_id"`)}
	}
	if _, ok := dgmc.mutation.DeviceID(); !ok {
		return &ValidationError{Name: "device_id", err: errors.New(`ent: missing required field "DeviceGroupMember.device_id"`)}
	}
	if _, ok := dgmc.mutation.AddedAt(); !ok {
		return &ValidationError{Name: "added_at", err: errors.New(`ent: missing required field "DeviceGroupMember.added_at"`)}
	}
	if _, ok := dgmc.mutation.DeviceGroupID(); !ok {
		return &ValidationError{Name: "device_group", err: errors.New(`ent: missing required edge "DeviceGroupMember.device_group"`)}
	}
	if _, ok := dgmc.mutation.DeviceID(); !ok {
		return &ValidationError{Name: "device", err: errors.New(`ent: missing required edge "DeviceGroupMember.device"`)}
	}
	return nil
}

func (dgmc *DeviceGroupMemberCreate) sqlSave(ctx context.Context) (*DeviceGroupMember, error) {
	if err := dgmc.check(); err != nil {
		return nil, err
	}
	_node, _spec := dgmc.createSpec()
	if err := sqlgraph.CreateNode(ctx, dgmc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	if _spec.ID.Value != nil {
		if id, ok := _spec.ID.Value.(*uuid.UUID); ok {
			_node.ID = *id
		} else if err := _node.ID.Scan(_spec.ID.Value); err != nil {
			return nil, err
		}
	}
	dgmc.mutation.id = &_node.ID
	dgmc.mutation.done = true
	return _node, nil
}

func (dgmc *DeviceGroupMemberCreate) createSpec() (*DeviceGroupMember, *sqlgraph.CreateSpec) {
	var (
		_node = &DeviceGroupMember{config: dgmc.config}
		_spec = sqlgraph.NewCreateSpec(devicegroupmember.Table, sqlgraph.NewFieldSpec(devicegroupmember.FieldID, field.TypeUUID))
	)
	if id, ok := dgmc.mutation.ID(); ok {
		_node.ID = id
		_spec.ID.Value = &id
	}
	if value, ok := dgmc.mutation.AddedAt(); ok {
		_spec.SetField(devicegroupmember.FieldAddedAt, field.TypeTime, value)
		_node.AddedAt = value
	}
	if nodes := dgmc.mutation.DeviceGroupIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   devicegroupmember.DeviceGroupTable,
			Columns: []string{devicegroupmember.DeviceGroupColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(devicegroup.FieldID, field.TypeUUID),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.DeviceGroupID = nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	if nodes := dgmc.mutation.DeviceIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   devicegroupmember.DeviceTable,
			Columns: []string{devicegroupmember.DeviceColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(device.FieldID, field.TypeUUID),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.DeviceID = nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// DeviceGroupMemberCreateBulk is the builder for creating many DeviceGroupMember entities in bulk.
type DeviceGroupMemberCreateBulk struct {
	config
	err      error
	builders []*DeviceGroupMemberCreate
}

// Save creates the DeviceGroupMember entities in the database.
func (dgmcb *DeviceGroupMemberCreateBulk) Save(ctx context.Context) ([]*DeviceGroupMember, error) {
	if dgmcb.err != nil {
		return nil, dgmcb.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(dgmcb.builders))
	nodes := make([]*DeviceGroupMember, len(dgmcb.builders))
	mutators := make([]Mutator, len(dgmcb.builders))
	for i := range dgmcb.builders {
		func(i int, root context.Context) {
			builder := dgmcb.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*DeviceGroupMemberMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, dgmcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, dgmcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, dgmcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (dgmcb *DeviceGroupMemberCreateBulk) SaveX(ctx context.Context) []*DeviceGroupMember {
	v, err := dgmcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (dgmcb *DeviceGroupMemberCreateBulk) Exec(ctx context.Context) error {
	_, err := dgmcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (dgmcb *DeviceGroupMemberCreateBulk) ExecX(ctx context.Context) {
	if err := dgmcb.Exec(ctx); err != nil {
		panic(err)
	}
}
