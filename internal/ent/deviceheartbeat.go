// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/bengobox/iot-service/internal/ent/device"
	"github.com/bengobox/iot-service/internal/ent/deviceheartbeat"
	"github.com/google/uuid"
)

// DeviceHeartbeat is the model entity for the DeviceHeartbeat schema.
type DeviceHeartbeat struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// DeviceID holds the value of the "device_id" field.
	DeviceID uuid.UUID `json:"device_id,omitempty"`
	// HeartbeatAt holds the value of the "heartbeat_at" field.
	HeartbeatAt time.Time `json:"heartbeat_at,omitempty"`
	// Status holds the value of the "status" field.
	Status string `json:"status,omitempty"`
	// BatteryLevel holds the value of the "battery_level" field.
	BatteryLevel int `json:"battery_level,omitempty"`
	// SignalStrength holds the value of the "signal_strength" field.
	SignalStrength int `json:"signal_strength,omitempty"`
	// Metadata holds the value of the "metadata" field.
	Metadata map[string]interface{} `json:"metadata,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the DeviceHeartbeatQuery when eager-loading is set.
	Edges        DeviceHeartbeatEdges `json:"edges"`
	selectValues sql.SelectValues
}

// DeviceHeartbeatEdges holds the relations/edges for other nodes in the graph.
type DeviceHeartbeatEdges struct {
	// Device holds the value of the device edge.
	Device *Device `json:"device,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// DeviceOrErr returns the Device value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e DeviceHeartbeatEdges) DeviceOrErr() (*Device, error) {
	if e.Device != nil {
		return e.Device, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: device.Label}
	}
	return nil, &NotLoadedError{edge: "device"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*DeviceHeartbeat) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case deviceheartbeat.FieldMetadata:
			values[i] = new([]byte)
		case deviceheartbeat.FieldBatteryLevel, deviceheartbeat.FieldSignalStrength:
			values[i] = new(sql.NullInt64)
		case deviceheartbeat.FieldStatus:
			values[i] = new(sql.NullString)
		case deviceheartbeat.FieldHeartbeatAt:
			values[i] = new(sql.NullTime)
		case deviceheartbeat.FieldID, deviceheartbeat.FieldDeviceID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the DeviceHeartbeat fields.
func (dh *DeviceHeartbeat) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case deviceheartbeat.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				dh.ID = *value
			}
		case deviceheartbeat.FieldDeviceID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field device_id", values[i])
			} else if value != nil {
				dh.DeviceID = *value
			}
		case deviceheartbeat.FieldHeartbeatAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field heartbeat_at", values[i])
			} else if value.Valid {
				dh.HeartbeatAt = value.Time
			}
		case deviceheartbeat.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				dh.Status = value.String
			}
		case deviceheartbeat.FieldBatteryLevel:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field battery_level", values[i])
			} else if value.Valid {
				dh.BatteryLevel = int(value.Int64)
			}
		case deviceheartbeat.FieldSignalStrength:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field signal_strength", values[i])
			} else if value.Valid {
				dh.SignalStrength = int(value.Int64)
			}
		case deviceheartbeat.FieldMetadata:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field metadata", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &dh.Metadata); err != nil {
					return fmt.Errorf("unmarshal field metadata: %w", err)
				}
			}
		default:
			dh.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the DeviceHeartbeat.
// This includes values selected through modifiers, order, etc.
func (dh *DeviceHeartbeat) Value(name string) (ent.Value, error) {
	return dh.selectValues.Get(name)
}

// QueryDevice queries the "device" edge of the DeviceHeartbeat entity.
func (dh *DeviceHeartbeat) QueryDevice() *DeviceQuery {
	return NewDeviceHeartbeatClient(dh.config).QueryDevice(dh)
}

// Update returns a builder for updating this DeviceHeartbeat.
// Note that you need to call DeviceHeartbeat.Unwrap() before calling this method if this DeviceHeartbeat
// was returned from a transaction, and the transaction was committed or rolled back.
func (dh *DeviceHeartbeat) Update() *DeviceHeartbeatUpdateOne {
	return NewDeviceHeartbeatClient(dh.config).UpdateOne(dh)
}

// Unwrap unwraps the DeviceHeartbeat entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (dh *DeviceHeartbeat) Unwrap() *DeviceHeartbeat {
	_tx, ok := dh.config.driver.(*txDriver)
	if !ok {
		panic("ent: DeviceHeartbeat is not a transactional entity")
	}
	dh.config.driver = _tx.drv
	return dh
}

// String implements the fmt.Stringer.
func (dh *DeviceHeartbeat) String() string {
	var builder strings.Builder
	builder.WriteString("DeviceHeartbeat(")
	builder.WriteString(fmt.Sprintf("id=%v, ", dh.ID))
	builder.WriteString("device_id=")
	builder.WriteString(fmt.Sprintf("%v", dh.DeviceID))
	builder.WriteString(", ")
	builder.WriteString("heartbeat_at=")
	builder.WriteString(dh.HeartbeatAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(dh.Status)
	builder.WriteString(", ")
	builder.WriteString("battery_level=")
	builder.WriteString(fmt.Sprintf("%v", dh.BatteryLevel))
	builder.WriteString(", ")
	builder.WriteString("signal_strength=")
	builder.WriteString(fmt.Sprintf("%v", dh.SignalStrength))
	builder.WriteString(", ")
	builder.WriteString("metadata=")
	builder.WriteString(fmt.Sprintf("%v", dh.Metadata))
	builder.WriteByte(')')
	return builder.String()
}

// DeviceHeartbeats is a parsable slice of DeviceHeartbeat.
type DeviceHeartbeats []*DeviceHeartbeat
