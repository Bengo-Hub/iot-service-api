// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/bengobox/iot-service/internal/ent/devicerule"
	"github.com/google/uuid"
)

// DeviceRule is the model entity for the DeviceRule schema.
type DeviceRule struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// TenantID holds the value of the "tenant_id" field.
	TenantID uuid.UUID `json:"tenant_id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// RuleType holds the value of the "rule_type" field.
	RuleType string `json:"rule_type,omitempty"`
	// TriggerConditions holds the value of the "trigger_conditions" field.
	TriggerConditions map[string]interface{} `json:"trigger_conditions,omitempty"`
	// Action holds the value of the "action" field.
	Action map[string]interface{} `json:"action,omitempty"`
	// IsActive holds the value of the "is_active" field.
	IsActive bool `json:"is_active,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the DeviceRuleQuery when eager-loading is set.
	Edges        DeviceRuleEdges `json:"edges"`
	selectValues sql.SelectValues
}

// DeviceRuleEdges holds the relations/edges for other nodes in the graph.
type DeviceRuleEdges struct {
	// Executions holds the value of the executions edge.
	Executions []*RuleExecution `json:"executions,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// ExecutionsOrErr returns the Executions value or an error if the edge
// was not loaded in eager-loading.
func (e DeviceRuleEdges) ExecutionsOrErr() ([]*RuleExecution, error) {
	if e.loadedTypes[0] {
		return e.Executions, nil
	}
	return nil, &NotLoadedError{edge: "executions"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*DeviceRule) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case devicerule.FieldTriggerConditions, devicerule.FieldAction:
			values[i] = new([]byte)
		case devicerule.FieldIsActive:
			values[i] = new(sql.NullBool)
		case devicerule.FieldName, devicerule.FieldRuleType:
			values[i] = new(sql.NullString)
		case devicerule.FieldCreatedAt, devicerule.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case devicerule.FieldID, devicerule.FieldTenantID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the DeviceRule fields.
func (dr *DeviceRule) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case devicerule.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				dr.ID = *value
			}
		case devicerule.FieldTenantID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field tenant_id", values[i])
			} else if value != nil {
				dr.TenantID = *value
			}
		case devicerule.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				dr.Name = value.String
			}
		case devicerule.FieldRuleType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field rule_type", values[i])
			} else if value.Valid {
				dr.RuleType = value.String
			}
		case devicerule.FieldTriggerConditions:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field trigger_conditions", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &dr.TriggerConditions); err != nil {
					return fmt.Errorf("unmarshal field trigger_conditions: %w", err)
				}
			}
		case devicerule.FieldAction:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field action", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &dr.Action); err != nil {
					return fmt.Errorf("unmarshal field action: %w", err)
				}
			}
		case devicerule.FieldIsActive:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field is_active", values[i])
			} else if value.Valid {
				dr.IsActive = value.Bool
			}
		case devicerule.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				dr.CreatedAt = value.Time
			}
		case devicerule.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				dr.UpdatedAt = value.Time
			}
		default:
			dr.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the DeviceRule.
// This includes values selected through modifiers, order, etc.
func (dr *DeviceRule) Value(name string) (ent.Value, error) {
	return dr.selectValues.Get(name)
}

// QueryExecutions queries the "executions" edge of the DeviceRule entity.
func (dr *DeviceRule) QueryExecutions() *RuleExecutionQuery {
	return NewDeviceRuleClient(dr.config).QueryExecutions(dr)
}

// Update returns a builder for updating this DeviceRule.
// Note that you need to call DeviceRule.Unwrap() before calling this method if this DeviceRule
// was returned from a transaction, and the transaction was committed or rolled back.
func (dr *DeviceRule) Update() *DeviceRuleUpdateOne {
	return NewDeviceRuleClient(dr.config).UpdateOne(dr)
}

// Unwrap unwraps the DeviceRule entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (dr *DeviceRule) Unwrap() *DeviceRule {
	_tx, ok := dr.config.driver.(*txDriver)
	if !ok {
		panic("ent: DeviceRule is not a transactional entity")
	}
	dr.config.driver = _tx.drv
	return dr
}

// String implements the fmt.Stringer.
func (dr *DeviceRule) String() string {
	var builder strings.Builder
	builder.WriteString("DeviceRule(")
	builder.WriteString(fmt.Sprintf("id=%v, ", dr.ID))
	builder.WriteString("tenant_id=")
	builder.WriteString(fmt.Sprintf("%v", dr.TenantID))
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(dr.Name)
	builder.WriteString(", ")
	builder.WriteString("rule_type=")
	builder.WriteString(dr.RuleType)
	builder.WriteString(", ")
	builder.WriteString("trigger_conditions=")
	builder.WriteString(fmt.Sprintf("%v", dr.TriggerConditions))
	builder.WriteString(", ")
	builder.WriteString("action=")
	builder.WriteString(fmt.Sprintf("%v", dr.Action))
	builder.WriteString(", ")
	builder.WriteString("is_active=")
	builder.WriteString(fmt.Sprintf("%v", dr.IsActive))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(dr.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(dr.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// DeviceRules is a parsable slice of DeviceRule.
type DeviceRules []*DeviceRule
